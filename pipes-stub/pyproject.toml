[project]
name = "hf-pipe"
version = "0.0.1"
description = "Reserved for an in-development Hugging Face data pipelines tool. Not yet functional."
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hf_pipe"]
