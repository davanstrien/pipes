# pipes

Name reserved for an in-development Hugging Face data pipelines tool — not yet
functional. Watch this space.

Note: the former Python stdlib `pipes` module was removed in 3.13 (PEP 594).
This package uses a different import name, so installing it never shadows the
stdlib module on older Pythons.
